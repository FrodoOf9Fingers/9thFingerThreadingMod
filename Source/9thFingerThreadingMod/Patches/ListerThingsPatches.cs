using Harmony;
using Verse;

namespace _9thFingerThreadingMod
{
    /*
    [HarmonyPatch(typeof(ListerThings), "ThingsInGroup")]
    static class ListerThingsPatch1
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }


    [HarmonyPatch(typeof(ListerThings), "ThingsOfDef")]
    static class ListerThingsPatch2
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }

    [HarmonyPatch(typeof(ListerThings), "ThingsMatching")]
    static class ListerThingsPatch3
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }

    [HarmonyPatch(typeof(ListerThings), "Contains")]
    static class ListerThingsPatch4
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }

    [HarmonyPatch(typeof(ListerThings), "Add")]
    static class ListerThingsPatch5
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }

    [HarmonyPatch(typeof(ListerThings), "Remove")]
    static class ListerThingsPatch6
    {
        [HarmonyPrefix]
        static bool Prefix(ListerThings __instance)
        {
            Blocker.block(__instance);
            return true;
        }

        [HarmonyPostfix]
        static void Postfix(ListerThings __instance)
        {
            Blocker.unblock(__instance);
        }
    }*/
}
